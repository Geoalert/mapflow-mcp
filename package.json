{
	"name": "mapflow-mcp",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"@types/bun": "^1.3.5"
	},
	"peerDependencies": {
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.25.1",
		"@turf/area": "^7.3.1",
		"@turf/helpers": "^7.3.1",
		"@turf/simplify": "^7.3.1",
		"zod": "^4.3.5"
	},
	"scripts": {
		"dev": "bun run --watch --hot src/server.ts",
		"lint:ts": "bunx tsc --noEmit",
		"lint": "biome check . --write",
		"format": "biome format . --write",
		"validate": "bun run lint:ts && bun run lint && bun run format",
		"test": "echo \"No tests specified\" && exit 0"
	}
}
